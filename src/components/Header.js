import React from 'react';

const Header = () => {
    return (
        <div>
            <h1>Integer Facts</h1>
            <h3>Enter a number for a trivial fact for that number!</h3>
        </div>
    );
};

export default Header;
