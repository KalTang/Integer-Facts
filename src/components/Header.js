import React from 'react';
import '../styles/header.css';

const Header = () => {
    return (
        <div className="header">
            <h1 className="title">Integer Facts</h1>
            <h3>Enter a number for a trivial fact for that number!</h3>
        </div>
    );
};

export default Header;
